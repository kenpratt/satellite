= Satellite

== Tasks

=== Now

* rspec testing framework
* fork ruby/git instead of monkeypatching it
* set up deployment (rake/capistrano/vlad)
* make a logo! (and a favicon to match)

=== Soon

* allow deletion of uploads
* allow renaming of uploads?
* fix incoming links during page rename operations
* page history view
* revert page to some previous version

=== Later?

* console that loads app + irb
* prettier web interface for manual conflict resolution
* user system/password protection? (but need db, cookies, etc, maybe just use httpauth)
* multiple remote repository support (instead of master/slave)
* tagging
* del.icio.us integration
* set up satellite-wiki.com
* switch from direct mongrel inheritance to rack adapter?
